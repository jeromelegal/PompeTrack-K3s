apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "airflow.name" . }}-dag-processor
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "airflow.labels" . | nindent 4 }}
    app.kubernetes.io/component: dag-processor
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "airflow.name" . }}
      app.kubernetes.io/component: dag-processor
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "airflow.name" . }}
        app.kubernetes.io/component: dag-processor
    spec:
      serviceAccountName: {{ include "airflow.serviceAccountName" . }}
      securityContext:
        runAsUser: 50000
        fsGroup: 50000
      initContainers:
        - name: wait-for-db
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z {{ include "airflow.name" . }}-postgresql 5432; do
                echo "Waiting for PostgreSQL...";
                sleep 3;
              done
      containers:
        - name: dag-processor
          image: {{ .Values.images.airflow.repository }}:{{ .Values.images.airflow.tag }}
          imagePullPolicy: {{ .Values.images.airflow.pullPolicy }}
          command: ["airflow", "dag-processor"]
          envFrom:
            - configMapRef:
                name: {{ include "airflow.name" . }}-config
          env:
            - name: AIRFLOW__DATABASE__SQL_ALCHEMY_CONN
              valueFrom:
                secretKeyRef:
                  name: airflow-db-conn
                  key: AIRFLOW__DATABASE__SQL_ALCHEMY_CONN
            - name: AIRFLOW__CORE__FERNET_KEY
              valueFrom:
                secretKeyRef:
                  name: airflow-core
                  key: AIRFLOW__CORE__FERNET_KEY
            - name: AIRFLOW__WEBSERVER__SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: airflow-core
                  key: AIRFLOW__WEBSERVER__SECRET_KEY
          resources:
            requests:
              cpu: {{ .Values.resources.dagProcessor.requests.cpu | quote }}
              memory: {{ .Values.resources.dagProcessor.requests.memory | quote }}
            limits:
              cpu: {{ .Values.resources.dagProcessor.limits.cpu | quote }}
              memory: {{ .Values.resources.dagProcessor.limits.memory | quote }}
          volumeMounts:
            - name: dags
              mountPath: /opt/airflow/dags
            - name: logs
              mountPath: /opt/airflow/logs
          livenessProbe:
            exec:
              command:
                - airflow
                - jobs
                - check
                - --job-type
                - DagProcessorJob
                - --local
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 5
      volumes:
        - name: dags
          persistentVolumeClaim:
            claimName: {{ include "airflow.name" . }}-dags
        - name: logs
          persistentVolumeClaim:
            claimName: {{ include "airflow.name" . }}-logs
