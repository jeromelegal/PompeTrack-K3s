apiVersion: v1
kind: Service
metadata:
  name: worker-stream
  namespace: pompetrack-core
  labels:
    app: worker-stream
spec:
  selector:
    app: worker-stream
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
