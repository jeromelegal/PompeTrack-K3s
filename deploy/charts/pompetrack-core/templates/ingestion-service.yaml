apiVersion: v1
kind: Service
metadata:
  name: ingestion
  namespace: pompetrack-core
  labels:
    app: ingestion
spec:
  selector:
    app: ingestion
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
