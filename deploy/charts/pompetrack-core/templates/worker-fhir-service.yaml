apiVersion: v1
kind: Service
metadata:
  name: worker-fhir
  namespace: pompetrack-core
  labels:
    app: worker-fhir
spec:
  selector:
    app: worker-fhir
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
