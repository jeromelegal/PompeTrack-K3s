apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: medplum-binary-pvc
  namespace: medplum
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 10Gi