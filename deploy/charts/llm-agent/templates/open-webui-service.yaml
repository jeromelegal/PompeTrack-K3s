apiVersion: v1
kind: Service
metadata:
  name: open-webui
  namespace: llm-agent
  labels:
    app: open-webui
spec:
  type: ClusterIP
  selector:
    app: open-webui
  ports:
    - name: http
      port: 8080
      targetPort: 8080